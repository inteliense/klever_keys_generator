# klever_keys_generator

The Java generator used in the klever_keys_cpp and klever_keys_c libraries.

This repo contains the Java source of the kleverkeys_generator.jar file within the klever_keys_cpp and klever_keys_c repos.

This repo also contains a built version of the jar.
